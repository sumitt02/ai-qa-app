# Optional: only needed if you're running the frontend separately from the backend
# (e.g., during npm run dev where you want to point at a non-default backend).
# In docker-compose this is not needed because nginx proxies /api → backend:8000.

# VITE_API_URL=http://localhost:8000
